# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Number of parallel workers for transcription and translation
# Default: 4
# Higher values = faster processing (but may hit rate limits)
# WORKERS=4

# GPT Translation Settings
# Model for translation: gpt-4o-mini (fast/cheap) or gpt-4o (best quality)
# Default: gpt-4o-mini
# GPT_MODEL=gpt-4o-mini

# Translation batch window in minutes
# Groups segments within time windows for better context
# Default: 5 minutes
# TRANSLATION_BATCH_MINUTES=5

# Maximum segments per batch (prevents GPT-4 formatting issues)
# Dialogue-heavy scenes will be split into multiple batches
# Default: 60
# MAX_SEGMENTS_PER_BATCH=60

# Enable GPT-4 correction pass for better quality
# Uses same time-based batching as translation
# Default: false
# ENABLE_CORRECTION=false
