# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Number of parallel workers for transcription
# Default: 4
# Higher values = faster processing (but may hit rate limits)
# WORKERS=4
